# ---------------------------------------------------------
# Toposcope hosted deployment configuration
# Copy this file to .env and edit the values.
# ---------------------------------------------------------

# --- Database ---
DB_PASSWORD=change-me-in-production

# --- API ---
API_PORT=8080
API_KEY=change-me-in-production

# --- Auth ---
# Options: none | api-key | oidc-proxy
AUTH_MODE=api-key

# --- Storage ---
# Options: local | s3 | gcs
STORAGE_BACKEND=local

# S3 settings (when STORAGE_BACKEND=s3)
# S3_BUCKET=my-toposcope-bucket
# S3_REGION=us-east-1
# S3_ENDPOINT=                    # Set for MinIO/R2 (e.g., http://minio:9000)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=

# GCS settings (when STORAGE_BACKEND=gcs)
# GCS_BUCKET=my-toposcope-bucket

# --- Web UI ---
WEB_PORT=3000
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080

# --- OIDC (uncomment oauth2-proxy in docker-compose.yml) ---
# OIDC_ISSUER_URL=https://accounts.google.com
# OIDC_CLIENT_ID=
# OIDC_CLIENT_SECRET=
# COOKIE_SECRET=           # Generate with: python3 -c 'import os,base64; print(base64.urlsafe_b64encode(os.urandom(32)).decode())'
