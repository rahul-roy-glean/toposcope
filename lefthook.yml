pre-commit:
  parallel: true
  commands:
    go-fmt:
      glob: "*.go"
      run: gofmt -l . | grep -v vendor
    go-vet:
      glob: "*.go"
      run: go vet ./...
    web-typecheck:
      glob: "web/**/*.{ts,tsx}"
      run: cd web && npx tsc --noEmit
